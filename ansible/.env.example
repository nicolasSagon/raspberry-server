# Variables d'environnement pour le déploiement Ansible
# Copiez ce fichier en .env et remplissez les valeurs

# Mot de passe pour déchiffrer le vault Ansible
# IMPORTANT: Utilisez des guillemets simples si le mot de passe contient des caractères spéciaux (&, $, !, etc.)
ANSIBLE_VAULT_PASSWORD='your_vault_password_here'

# Optionnel : Variables supplémentaires
# ANSIBLE_HOST_KEY_CHECKING=False
# ANSIBLE_SSH_PIPELINING=True
